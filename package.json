{
  "name": "confluence-cluster-stats",
  "version": "1.0.0",
  "description": "package to obtain cluster stats for dev and prod",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "D'Ante Barnes",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.10",
    "request": "^2.88.2"
  }
}
